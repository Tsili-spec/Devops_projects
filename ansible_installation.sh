sudo apt update
sudo apt-add-repository ppa:ansible/ansible #installing ansible repo
sudo apt install ansible -y #installing ansible from ansible repo
#good pratice is to create an ansible user but here we are not doing it because we want ansible and terraform to have same user
